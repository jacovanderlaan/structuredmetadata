# Image catalog — structuredmetadata.com

> Naamconventie: `<slug>-<rol>.png` (slug=home voor index). Technischer blueprint-variant
> van de SBM line-art familie (denser ERD/schema-look), gegenereerd via OpenAI gpt-image-1.
> Brand: navy #0f172a, blauw #2563eb, off-white #f7f8fa.

## home (index.html)
- `home-hero.png` — een ERD-schema van vele verbonden tabellen; vier hubs blauw uitgelicht in de keten **model → layer → entity → attribute**, met tientallen lege tabellen eromheen (suggereert het 268-tabellen-model). Blueprint/database-diagram-look.
- `home-01.png` — **data, not code**: een metadata-database-store links → blauwe pijlen → gegenereerde output-files rechts (DDL/dbt/pipelines/docs/diagrams). Labels "METADATA" + "GENERATED". Blauwdruk-raster-achtergrond. "Metadata is de bron; de artifacts zijn gegenereerde views."

## Favicon
- `favicon.svg` — gestructureerd schema-grid (blauwe tabelbalk + cellen) op navy achtergrond.
